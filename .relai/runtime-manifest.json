{
  "schema_version": "relai.runtime_manifest.v1",
  "env": [
    {
      "name": "OPENAI_API_KEY",
      "required": true,
      "sensitive": true,
      "reason": "Needed by the LangChain OpenAI chat clients used by the router and specialist agents."
    },
    {
      "name": "OPENAI_MODEL",
      "required": false,
      "sensitive": false,
      "reason": "Optional override for the chat model name; defaults to gpt-5.4 when unset."
    },
    {
      "name": "TUTOR_LEARNER_ID",
      "required": false,
      "sensitive": false,
      "reason": "Optional stable learner identifier when you want simulator runs to reuse the same learner profile."
    }
  ],
  "files": [
    {
      "path_env": "TUTOR_DB",
      "required": false,
      "reason": "Optional SQLite file path for durable learner progress; the adapter creates an isolated simulator-local database when unset."
    },
    {
      "path_env": "RELAI_SIMULATOR_CHECKPOINT_DB",
      "required": false,
      "reason": "Optional SQLite file path for the LangGraph checkpoint store; the adapter creates an isolated simulator-local checkpoint database when unset."
    }
  ]
}
